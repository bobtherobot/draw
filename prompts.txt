lets consolidate what we've done so far so we can continue working later.


---
that's the correct behaviour, but we don't want to just change tools. Rather, we want the newly drawn shape to be in edit mode, while maintaining the pen tool active, so we can start drawign a new shape if we want.

---

so when in editing node mode, when multiple nodes are selected, we are completing the full bubble/capture on click, because we have to be a little more accurate with our clicks and selections. So the behaviour should not be like how we're selecting objects, where mouse down and drag to move an object

---

so when in editing node mode, when multiple nodes are selected, we need a unique move behaviour (slightly different from how we deal with selecting full objects/shapes).

Here, since we go through the full mouse bubble/capture phase for each node we choose when the shift key is selected to add additional nodes to our selection, where each node is clicked (mouse down, up) explicitly.

We want to maintain all of the nodes selected, so if we click on an already selected node (again), we shouldn't reset what is selected to only the last selected node -- we should maintain all selected nodes, we are only updating the primary node that we will will use as the reference for moving the entire list of selected nodes.

--- 

after drawing a shape with the pen tool, which causes the newly drawn shape to enter into node edit mode, when the mouse is hovering over the shape, the cursor should change to the arrow, and if the user starts to adjust any of the nodes on the newly drawn shape, we can assume they want to enter into "direct selection" mode and we can automatically swithc tools to "direct selection". However, when the hover outside of the shape, we can assume the user intent is to start drawing a new shape, and stay in pen mode.

So the next click action after finishing drawing will determine the intent:
1. if the user clicks on any of the nodes in the recently drawn shape, then automatically switch to "direct selection" mode (change tools).
2. if the user doesn't click on any previously drawn node, then they are remaining in pen mode and will be drawing a new shape.

We shouls also change the cursor to a hollow (white) cursor arrow when in edit node mode, and a normal black cursor arrow when in "selection" tool.

---
ok, that's good you're selecting the correct node. However, since this node is now selected (auto selected), if the user clicks and drags this auto-selected node, it should move. Currently, clicking the auto-selected node is de-selecting it -- it should remain selected so that the user can move it.


---
ok, we're getting close, but when i click (mouse down) on the auto-selected node and immediately start dragging, it doesn't drag. I have to click it before i can drag it. I'd like to be able to drag it immediately (since it's already selected).

---

NODE-EDITING - add marquee selection logic to this doc.

---

next we want to provide the ability to select multiple nodes at once using a marquee selection (drag a bounding box to select multiple nodes at once).

---

currently, the system has been working on node editing based on one object at a time. I'd like to be able to select more than one shape and be able to edit nodes in multiple shapes at a time.

---

lets add a new mode "wireframe", which will change how we see objects on the artboard. When in wireframe mode, all shapes (objects) will render as wireframes and nodes points. So no fill will be applied to anything, all we will see is the wireframes.

...

lets add a new panel for global controls (we probably want a better name for this, since we're already have a "controls" panel).

In this global panel, we'll include broader controls for operations. At this point, we'll only want to include a radio group (displayed as buttons) that control the view mode.

This global panel will be a horizontal bar, just below the main menu, and should be dock-able, and draggable. Where we can dock it to the top, or drag it away and it will be free-floating. When it gets dragged close to the top menu it will automatically snap to the top and be docked. When dragging it away from the top it'll undock.

This panel doesn't need a title bar, only a side "handle" for dragging and docking purposes.

This view mode will have 2 options: "normal" or "wireframe" (we'll use icons for these).

This new mode "wireframe", will change how we see objects on the artboard. When in wireframe mode, all shapes (objects) will render as wireframes and nodes points. So no fill nor stroke will be applied to anything, all we will see is the wireframes.

TODO: tool controls panel - where when a tool is selected, tool-specific controls will be available.


--- TODO

when in wireframe mode, the behaviour of our app will significantly change.

We will be treating everything on the artboard as if we are in direct select mode -- as if all objects were selected and we can edit all nodes at once.

The "select" tool will still allow us to select individual objects, but the user should be able to select any node (control point) or shape line to activate that shape 

---



  1. Node tool (A) — direct selection & bezier handle editing (stub exists in
  src/tools/node.js)
  2. Scale handles — drag corners of selection bounding box to resize shapes
  3. Rotate handle — rotate selected shapes
  4. Text re-editing — double-click an existing text shape to re-enter edit mode
  5. Font controls — font size / font family in the style panel
  6. Group / Ungroup (Cmd+G / Cmd+Shift+G) — stubs exist, just log to console
  7. Double-click group — enter isolated group editing mode
  8. Display units — pt/mm/cm/in selector exists in Options but coordinates still
  always show px
